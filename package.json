{
  "name": "dice2win",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ achiko/dice2win",
    "create-local": "graph create --node http://localhost:8020/ achiko/dice2win",
    "remove-local": "graph remove --node http://localhost:8020/ achiko/dice2win",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 achiko/dice2win"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.12.0",
    "@graphprotocol/graph-ts": "0.11.0"
  }
}
